import { AssetsimagePipe } from './assetsimage.pipe';

describe('AssetsimagePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsimagePipe();
    expect(pipe).toBeTruthy();
  });
});
